<!doctype html>
<html>
  <head>
    <title>{{ shop.name }}</title>
    {{ content_for_header }}
  </head>
  <body>
    <main>{{ content_for_layout }}</main>
    {{ content_for_footer }}
  </body>
</html>
